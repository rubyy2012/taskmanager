import React from 'react'

let initialValue = {
  user: null, // For example, you can set it to null
  loading: false,
  error: null,
}
const UserReducer = (initialValue, action) => {}

export default UserReducer
